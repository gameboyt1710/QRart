generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Artwork {
  id        String   @id
  title     String?
  mimeType  String
  imageData Bytes
  createdAt DateTime @default(now())

  @@index([id])
}
