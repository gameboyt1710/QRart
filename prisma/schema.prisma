generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Artwork {
  id        String   @id
  imageData Bytes
  mimeType  String
  createdAt DateTime @default(now())
}
