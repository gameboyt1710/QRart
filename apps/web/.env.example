# Backend API URL
# In development, Vite proxies /api to localhost:4000
# In production (Railway), the backend serves the web UI, so use relative paths
# Leave empty for production or use the full URL for development
VITE_API_BASE_URL=
